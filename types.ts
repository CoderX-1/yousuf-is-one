
export interface Milestone {
  id: number;
  title: string;
  description: string;
  icon: string;
}

export interface GuestMessage {
  id: string;
  name: string;
  message: string;
  date: string;
}

export interface Trend {
  title: string;
  category: string;
}
